# Set to "enabled" to turn on MSW request mocking for both the server
# (Server Components, Route Handlers) and the browser (Client Components).
# Leave unset/empty to talk to real APIs.
NEXT_PUBLIC_API_MOCKING=
